SECRET_KEY=cambia-esto-por-una-clave-secreta-larga-y-unica
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# En Railway, esta variable la provee automáticamente el plugin de PostgreSQL.
# En local se deja vacía para usar SQLite.
DATABASE_URL=

# Railway la inyecta automáticamente en producción; no es necesario definirla en local.
RAILWAY_PUBLIC_DOMAIN=
